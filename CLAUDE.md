# sys-dashboard

Terminal system monitor dashboard (CPU, RAM, temps, disk, network, processes).

## Stack
- Python 3.12 / uv
- psutil + rich

## Run
```
cd ~/soft/sys-dashboard
uv run python dashboard.py
```
